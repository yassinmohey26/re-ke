import TranslationsClient from './TranslationsClient';

export const metadata = {
  title: 'Translations — ZAIMOZ Admin',
};

export default function TranslationsPage() {
  return <TranslationsClient />;
}
